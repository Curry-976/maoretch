{
  "name": "maoretech",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "cd backend && bun install && cd ../webapp && bun install",
    "build:web": "cd webapp && bun install && bun run build && rm -rf ../backend/public && mv dist ../backend/public",
    "build:db": "cd backend && bun run prisma:generate && bun run prisma:push",
    "build": "bun run build:web && bun run build:db",
    "start": "cd backend && bun run start",
    "dev:web": "cd webapp && bun run dev",
    "dev:api": "cd backend && bun run dev"
  }
}
